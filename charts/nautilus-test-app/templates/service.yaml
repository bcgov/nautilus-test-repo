apiVersion: v1
kind: Service
metadata:
  name: {{ include "nautilus-test-app.fullname" . }}
  labels:
    {{- include "nautilus-test-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nautilus-test-app.selectorLabels" . | nindent 4 }}
